import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    secondary: {
      light: '#DCF5F2',
      main: '#00b4aa',
    },
    background: {
      paper: '#ffffff',
      default: '#f8f7fc',
    },
  },
})
